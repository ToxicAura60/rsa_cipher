name: rsa_cipher
description: rsa_cipher is a powerful and easy-to-use package for RSA encryption and decryption in Dart.
version: 1.2.2
repository: https://github.com/ToxicAura60/rsa_cipher

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  pointycastle: ^4.0.0
  asn1lib: ^1.6.5

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
